<!-- app/views/reviews/_form.html.erb -->
<div class="flex-box">
  <%= simple_form_for([ @hairstyle, comment ]) do |form| %>
    <%= form.input :content, as: :text %>
    <%= form.button :submit %>
  <% end %>
</div>
