plusCircle = document.getElementById("<%= dom_id(@hairstyle, :bookmark)%>");
//link to the saved_hairstyle path and pass an instance of saved hairstyle
plusCircle.innerHTML = `<%= link_to saved_hairstyle_path(@saved_hairstyle), method: :delete do %>
                     <i class="fas fa-bookmark"></i>
                 <% end %>`;


//get the element you want to change i.e target it with get element by id
// then take that element and shove some new html and ruby in there in place of
// what is on the html page at the moment.





